// TODO: Turn this into a Scala test case.
// TODO: Also test that an independent trait cannot be used as a component type.

trait Stat
struct Strength implements Stat
struct Dexterity implements Stat

struct Hero {
  // Strength and Dexterity cannot be both components of Hero since they share a supertype.
  component Strength
  component Dexterity
}
