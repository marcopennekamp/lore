class A
class B

class ContReq {
  a: A

  // Illegal: Constructors must end in a continuation.
  foo(a: A) { }
}

// Illegal: Constructor calls are cyclic.
class Cyclic {
  a: A
  b: B

  foo(a: A) {
    this.bar(a)
  }

  bar(a: A) {
    this.foo(a)
  }

  baz(a: A) {
    this(a, B())
  }
}
