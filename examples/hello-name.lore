action hello(value: Any)

action hello(name: String) {
  println('Hello, $name.')
}

action hello(id: Int) {
  println('Hello, anonymous #$id.')
}

action test() {
  let names: [String | Int] = ['world', 5, 'marco']
  names = append(names, 'console')
  names = append(names, 100)
  for (name <- names) {
    hello(name)
  }
}
