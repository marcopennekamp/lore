module lore.Symbol

// TODO (pyramid): This must not be Any, but rather any kind of symbol. We don't have a type for that yet, though.
func name(symbol: Any): String = intrinsic[String]('lore.symbol.name', symbol)
