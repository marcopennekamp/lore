module lore.Symbol

// TODO: This must not be Any, but rather any kind of symbol. We don't have a type for that yet, though.
func name(symbol: Any): String = dynamic[String]('Lore.symbols.name', symbol)
