module lore

use lore.core.panic

// TODO (assembly): Support maps in the VM.

module Map do
//  @where K, V
//  func get!(kvs: #[K -> V], key: K): V = panic('`lore.Map.get!` is not supported yet.')
end
