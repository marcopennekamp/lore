module lore

use lore.core.panic

// TODO (assembly): Support maps in the VM.

module Map do
//  @where K, V
//  func get!(kvs: #[K -> V], key: K): V = panic('Not supported yet.') //intrinsic[V]('Lore.maps.get', kvs, key)
end
