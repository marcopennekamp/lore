function identity(x: A): A where A = x
function identity(x: Int): Int = x
function identity(str: String): String = str

function test(): [Real | String | Boolean] = [identity(0), identity(1.2), identity('Hello'), identity(true)]
