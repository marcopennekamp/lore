use lore.String

// This ensures that function types are parsed with right associativity.
func function_associativity(): Int = do
  let f: Int => String => Int = n => s => n + String.length(s)
  f(5)('associativity')
end

func test(): Int = function_associativity()
