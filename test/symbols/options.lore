// TODO: We won't implement options using symbols, so this test case needs to be moved and rewritten.

func test(): [String] = do
  let a = 'hello options'
  let b = #none
  let c: String | #none = 'test'
  let d: String | #none = #none

  [
    to_string(isEmpty(a)),
    to_string(isEmpty(b)),
    to_string(isEmpty(c)),
    to_string(isEmpty(d)),

    get_or_else(a, 'fallback'),
    get_or_else(b, 'fallback'),
    get_or_else(c, 'fallback'),
    get_or_else(d, 'fallback')
  ]
end
