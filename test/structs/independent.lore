independent trait C

struct CA implements C
function toString(c: CA): String = 'CA'

struct CB implements C
function toString(c: CB): String = 'CB'

struct E {
  component CA
  component CB
}

function test(): [String] = {
  let e = E(CA(), CB())
  [toString(e.CA), toString(e.CB)]
}
