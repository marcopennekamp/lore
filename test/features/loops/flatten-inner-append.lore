use lore.Enum.map

func append3(list: [Int]): [Int] = list :+ 3

func test(): [Int] = do
  let xss = [
    [1, 2],
    [],
    [3, 7, 5],
    [4, 4, 4],
  ]
  for xs <- xss, x <- append3(xs) yield x
end
