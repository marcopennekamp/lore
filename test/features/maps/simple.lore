use lore.Tuple.[first, second]

// TODO (assembly): Needs map support.

let dummy_map: #[String -> Int] = #['a' -> 0, 'b' -> 1, 'c' -> 2]

func test(): [String] = do
  // TODO: This is, understandably, not valid and needs a different kind of runtime type handling as outlined in runtime-types.
  //let empty: String -> Int = #[]

  for entry <- dummy_map yield '${first(entry)} is ${second(entry)}'
end
