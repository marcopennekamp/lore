// Note: The preceding `where` would remove some noise from the `func` line. Currently, defining parametric functions
// is a bit messy as so much information is condensed in a single line.
@where A
func string_join(list: [A], separator: String): String = do
  if empty?(list) then return ''
  concat(
    init(list) |> concat_map(a => '$a$separator'),
    last(list) |> to_string
  )
end
