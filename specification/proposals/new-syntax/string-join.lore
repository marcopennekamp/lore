// The preceding `where` would remove some noise from the `func` line. Currently, defining parametric functions is a
// bit messy as so much information is condensed in a single line.
@where A
func stringJoin(list: [A], separator: String): String = do
  if !isEmpty(list)
    concat(
      init(list) |> concatMap(a => '$a$separator'),
      last(list) |> toString
    )
  end else ''
end
